int p=0;
	    int result(int n)
	    {
	        for(int i=2;i<sqrt(n);i++)
	        {
	            if(n%i==0)
	            {
	                p++;
	                n=n/i;
	            }
	        }
	        if(n>1)
	        {
	            p++;
	        }
	        return p;
	    }
